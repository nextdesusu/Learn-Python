from math import sqrt, factorial
from Newton_transform import cube_root

def cons(a, b):
    return pow(2, a) * pow(3, b)

def power(num, x):
    while value % base == 0:
        pass

def fact(num, x):
    counter = 0
    while num % x == 0:
        num /= x
        counter += 1
    return counter

def car(z):
    return fact(z, 2)

def cdr(z):
    return fact(z, 3)


b = cons(11, 23)
print(b)

print(car(b))
print(cdr(b))

